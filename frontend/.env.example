# URL of the deployed backend. When running locally this is http://localhost:8000.
# When deployed, set this to your Render (or Railway) backend URL.
# e.g. VITE_API_URL=https://ics-resume-analyzer-api.onrender.com
VITE_API_URL=http://localhost:8000
